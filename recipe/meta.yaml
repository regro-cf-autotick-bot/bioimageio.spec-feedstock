{% set name = "bioimageio.spec" %}
{% set version = "0.5.14.1" %}
{% set python_min = "3.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/bioimageio_spec-{{ version }}.tar.gz
  sha256: 0284992300194e6ac74b7d8ea534c14472b8574f7f8a29214b71c08ecf20a8ee

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install --no-deps . -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - email-validator
    - annotated-types >=0.5.0,<1
    - email_validator
    - exceptiongroup
    - genericache ==0.5.2
    - httpx
    - imageio
    - loguru
    - markdown
    - numpy >=1.21
    - packaging >=17.0
    - platformdirs
    - pydantic >=2.10.3,<3
    - pydantic-core
    - pydantic-settings >=2.5,<3
    - python >={{ python_min }}
    - python-dateutil
    - rich
    - ruyaml
    - tifffile >=2020.7.4
    - tqdm
    - typing_extensions
    - zipp


test:
  imports:
    - bioimageio.spec
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/bioimage-io/spec-bioimage-io
  summary: Parser and validator library for BioImage.IO specifications
  dev_url: https://github.com/bioimage-io/spec-bioimage-io
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - constantinpape
    - FynnBe
    - k-dominik
    - oeway
